version: '3.8'
services:
  thaimaster:
    build:
      context: .
      args:
        - API_KEY=${API_KEY}
    ports:
      - "3000:3000"
    environment:
      - API_KEY=${API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped